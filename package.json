{
  "name": "the-awesome-challenge-list",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "ws": "^8.21.2"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "validate:data": "python scripts/validate_data.py",
    "test:api": "node --test tests/api.test.mjs"
  }
}
